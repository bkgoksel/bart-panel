#pragma once
// Copy to secrets.h and fill in. secrets.h is gitignored.
// Wi-Fi networks, tried in order.
static const char *const WIFI_NETWORKS[][2] = {
    {"first-choice-ssid", "password"},
    {"fallback-ssid", "password"},
};
